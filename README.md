# Installation

```
git clone git@github.com:nguyentbinh/kitty-config.git ~/.config/kitty
cd ~/.config/kitty
git submodule update --init --recursive
```

```
ln -s ~/.config/kitty/kitty-themes/themes/Molokai.conf ~/.config/kitty/theme.conf
```
